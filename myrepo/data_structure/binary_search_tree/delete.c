#include "bst.h"

/**
 * delete - delete a value in a binary tree
 * @root: the root of the tree
 *
 * Return: return 0 on success
 */
int delete(node **root)
{
	node *tmp;
	tmp = *root;

	if (tmp->n = value)
	{
		tmp = merge_tree(tmp->left, tmp->right);
	}
	else
	{
		current = tmp;
		if (tmp->n)
		{



